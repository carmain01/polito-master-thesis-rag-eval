You are an expert evaluator assessing the recall of retrieved context chunks.
Your task is to extract key statements from the reference ground truth answer, and check if each statement can be fully found or inferred from the provided context chunks.

Ground Truth Reference Answer:
${ground_truth}

Context Chunks:
${contexts}

Return a JSON object in this exact format:
{
  "statements": [
    {
      "statement": "string (a key fact or statement extracted from the ground truth)",
      "is_covered": boolean (true if the statement is covered by the contexts, false otherwise)
    }
  ],
  "reason": "string (brief explanation)"
}

IMPORTANT: Return ONLY the JSON object above. Do not include any other text, markdown formatting, or code fences.
